function y = affmap(x,beta,gamma)
%Affmap function is a linear transformation function y = betax+gamma where x is
%input and y is the output
%   Input:   x: input of scalar/vector/matrix type
%   Output:  y: output of scalar/vector/matrix type
    y = beta*x+gamma;
end

